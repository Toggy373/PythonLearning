# This will iterate the numbers and columns assigning seats names 
num_rows = int(input())
num_cols = int(input())

for row in range(1, num_rows):
    for col in range(num_cols):
        seat = f"{row}{chr(ord('A') + col)}"
        print(seat, end=' ')
print()
